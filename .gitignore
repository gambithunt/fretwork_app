# Xcode
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.build/
.swiftpm/

# Code coverage artifacts
*.profraw
*.profdata

# macOS
.DS_Store

# Leftover scaffold project — has its own git repo, not part of this one
/fretwork/

# Release artifacts
/build/

# Recorded note-sample masters. Irreplaceable without another session with the
# guitar, and far too large for the repo — back them up alongside the signing
# keys (see docs/releasing.md).
/SampleMasters/
